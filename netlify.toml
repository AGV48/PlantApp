# Netlify Configuration
[build]
  command = "cd PlantApp_Front && npm install && npm run build"
  publish = "PlantApp_Front/dist/plant-app-front/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
